# 性能优化

## 核心原则

- 对内存敏感，永远秉持最低内存使用原则，将内存使用压缩到极致。
- 对 CPU 计算量敏感，永远秉持简单即是正义、复杂就是垃圾的原则，降低 CPU 的高频使用率。
- 对 Drawcall 敏感，重视合批的优先级，永远秉持能合则合的原则，尽量将 DC 压到最低。

## 指导规则

1. 避免每帧创建节点/组件。
2. 禁止在 update/lateUpdate 中执行耗时操作或使用复杂算法。
3. UI 动画优先使用 tween。
4. 大量对象复用对象池技术优化。
